<!DOCTYPE html>
<html lang="en">
    <head></head>
    <body>
        <h1>{{ object_row.full_name }}, a {{ object_row.age }} year old, 
            {{ object_row.race }} {{ object_row.gender|lower }} died on {{ object_row.date }}
            in a {{ object_row.type|lower }} at {{ object_row.address }} in {{ object_row.neighborhood }}.
        </h1>
    </body>
</html>
